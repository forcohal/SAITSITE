/**
 * Returns a chat response for the given question.
 * TODO: Replace this stub with the actual RAG pipeline in the next phase.
 */
async function getChatResponse(question) {
  // Temporary placeholder response — will be replaced with RAG retrieval + LLM call
  return `This is a placeholder response. The RAG pipeline is not connected yet. You asked: "${question}"`;
}

module.exports = { getChatResponse };
